# Cloudflare Origin Certificate Placeholder

Put the real Cloudflare origin certificate files here in the private `.local/`
copy:

- `.local/role_vars/caddy/cf-certs/server.crt`
- `.local/role_vars/caddy/cf-certs/server.key`

Do not commit the real certificate private key.
