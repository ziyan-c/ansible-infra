services:
  xray:
    image: "{{ xray_image }}"
    container_name: xray
    restart: unless-stopped
    ports:
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./config.json:/usr/local/etc/xray/config.json:ro
